# Проверка ввода LU4 — 2026-09-24

Проверено на локальном клиенте, HWND 59053092, PID 16288.
Тестовый процесс запущен через Windows UAC; уровни целостности обоих процессов
12288. F10 зарегистрирована, проверки фокуса и границ ActionController включены.
Все события отправлены штатным SendInput через ActionController.

1. Одно нажатие R, запрошено 300 мс, в логе 313 мс между отправками.
2. Один клик ЛКМ по значку Wind Strike, запрошено 300 мс, в логе около 297 мс.
   Наведение подтверждено появлением подсказки «Wind Strike Lv. 1 (MPCost: 9)».

Windows приняла down/up в обоих тестах. На сохранённых кадрах MP остаются 98/98,
HP выбранного Gremlin — 58/58; новой записи применения навыка нет. Уже имевшаяся
строка «You use Wind Strike» присутствует ДО тестов и не считается успехом.
Цель была выбрана до начала проверки: выбор монстра кликом отдельно не проверялся.

Артефакты: `artifacts/lu4-probe-before.png`, `lu4-probe-after-0..3.png`,
`lu4-probe-mouse-before.png`, `lu4-probe-mouse-after-0..3.png`.
Журнал: `logs/agent.log`, интервалы 15:36:34–15:36:42 и 15:37:17–15:37:25.

Вывод: отсутствие наблюдаемого применения навыка воспроизведено при одинаковых
правах и проверенном фокусе. Конкретный механизм фильтрации клиентом не установлен;
результат не доказывает наличие определённого античита. Обход защиты не выполнялся.
Числа длительности отражают часы процесса и планирование Windows, а не время
обработки событий игрой. Программные unit-тесты не являются приёмкой управления LU4.
